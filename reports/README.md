# Reports Directory

This directory will contain stress test results including:
- CSV files with latency measurements
- JSON files with test summaries
- PNG visualization charts
- Comprehensive dashboard images

Results are organized by scenario (client count).
